/**
 * CuesView - Region list, playlist mode
 * Tap to jump, loop counts for live performance
 * See CUE_LIST_FEATURE.md for full spec
 */

import type { ReactElement } from 'react';

export function CuesView(): ReactElement {
  return (
    <div className="min-h-screen bg-gray-950 text-white p-4 flex flex-col items-center justify-center">
      <h1 className="text-2xl font-bold mb-4">Cues View</h1>
      <p className="text-gray-400">Cue list coming soon</p>
    </div>
  );
}
